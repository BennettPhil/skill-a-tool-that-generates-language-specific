# a-tool-that-generates-language-specific

Idea prompt: A tool that generates language-specific .gitignore files. Given a language, framework, or tool name, it produces a comprehensive .gitignore with annotated sections explaining each pattern.

## Context
Should support at least 20 common languages/frameworks. Can combine multiple targets (e.g., 'node,macos,vscode').

## Notes
Generated using builder `pragmatic-builder` (c47497f1-04b2-46be-a320-e4ee4e18a360) with fitness 0.374.